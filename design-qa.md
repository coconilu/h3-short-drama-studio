# Design QA — 剧本 Agent 工作区

## 视觉真值与实现证据

| 项目 | 证据 |
|---|---|
| 选定方案 | `docs/design/script-studio-selected-agent-reference.png`（1487 × 1058） |
| 浏览器实现 | `docs/design/script-studio-browser-1440x1024.png`（CSS viewport 1440 × 1024，device scale factor 1） |
| 全视图对比 | `docs/design/script-studio-qa-side-by-side.png`（源图归一化到 1440 × 1024 后与实现并排） |
| 页面状态 | “剧本开发”导航激活；S02 场景选中；Kimi 场景草稿已完成、尚未应用 |

## 对比结论

| 维度 | 结论 |
|---|---|
| 信息架构 | 实现保留了整集操作区、三幕结构列、场景卡片、右侧检查器和 Agent 任务区；主次层级与选定方案一致。 |
| 视觉语言 | 延续近黑画布、暖金主操作、低对比边框和绿色连接状态；未引入渐变或无关强调色。 |
| 字体与密度 | 标题、场景编号、状态标签和检查器表单均保持桌面生产工具密度，1440px 下无需横向滚动。 |
| 真实数据差异 | 方案图包含 10 个示意场景；实现只显示当前项目真实存在的 3 个场景，因此下方留白更大。这是数据约束，不以虚构场景补齐。 |
| 全局壳层差异 | 顶栏继续显示全局 ComfyUI 运行状态；剧本页标题区另显示本地 Agent 连接状态，避免把两种服务状态混为一谈。 |
| 控件与图标 | 导航、场景选择、手工编辑、Agent 范围选择、差异审核、接受和拒绝均为真实交互，不是装饰按钮。 |

## 交互与数据验证

- 已验证从侧栏进入“剧本开发”、切换场景、打开手工编辑、选择 Codex/Kimi、查看逐字段差异。
- 已通过真实 Codex CLI 和 Kimi Code CLI 各完成一条场景提案；两条提案均保留为待审核状态。
- 前端未替用户点击“接受并创建版本”；后端单元测试覆盖场景、幕和整集接受路径，以及生产镜头不被改写的约束。
- 生成命令以参数数组启动，不经过 shell；Agent 运行目录与结果目录位于受控 runtime 路径。

## 比较历史与遗留项

| 级别 | 发现 | 处理 |
|---|---|---|
| P1 | Kimi CLI 的初始参数组合与当前版本不兼容 | 移除冲突参数，改用 `stream-json` 并从 assistant 事件提取结构化提案；真实运行通过。此项属于功能修复，不计为视觉迭代。 |
| P2 | 场景切换时任务区可能回退显示无关场景的草稿 | 调整关联优先级为当前场景 → 当前幕 → 整集，不再回退到无关场景。 |
| P3 | 真实项目仅有三个场景，画布密度低于方案图 | 明确保留真实空白；后续由用户剧本或 Agent 提案自然扩展，不注入 mock 数据。 |

最终视觉与交互检查未发现待处理的 P0/P1/P2 问题。

## 实现检查表

- [x] 整集、幕/章、场景/小节三级 Agent 入口可用
- [x] Codex 与 Kimi 本地 Provider 探测和真实运行通过
- [x] Agent 提案默认待审，接受后才创建新版本
- [x] 生产镜头不随剧本提案自动覆盖
- [x] 浏览器视觉、主要交互和控制台完成检查
- [x] 后端测试与前端生产构建通过

## P9 分镜同步增量验收

| 项目 | 证据 |
|---|---|
| 视觉基线 | `docs/design/script-studio-selected-agent-reference.png`（1487 × 1058） |
| 正式数据草稿门禁 | `docs/design/script-storyboard-sync-draft-gate-1440x1024.png`（1440 × 1024） |
| 同步预览 | `docs/design/script-storyboard-sync-preview-1440x1024.png`（1440 × 1024，隔离数据库副本） |
| Browser 验收 | 正式 `4173` 验证草稿禁用；隔离 `8766` 验证锁定、预览、确认、进入分镜 |

五项视觉对比：

1. 全局壳层、暖金操作色、近黑画布、低对比边框与既有剧本工作区一致。
2. 新的“同步到分镜”属于必需下游动作，放在锁定按钮下方，没有改变三幕主画布和右侧检查器层级。
3. 预览弹窗沿用 Agent 审核弹窗的矩形容器、宋体标题和紧凑表格，不引入新的卡片或渐变语言。
4. 变更统计、逐镜映射和三条安全策略在 1440 × 1024 首屏内完整可见，确认按钮保持单一暖金主操作。
5. 剧本页在 375px 视口自动收起导航、纵向排列三幕；`clientWidth` 与 `scrollWidth` 均为 375，无横向溢出。

文案差异检查：新增的“同步到分镜”“新增/更新/不变/受保护/保留”和安全说明均为本次生产流程的必要可见文案；未改名或移除既有剧本、Agent 与锁定操作。

交互结果：正式剧本 v4 草稿不能同步；隔离副本锁定为 v5 后预览得到 3 个更新，确认后显示“v5 已同步”，进入分镜可看到新标题，原英文 H3 提示词仍保留。两个浏览器页面均无 console error/warning。

有意差异：选定概念图没有展示同步弹窗；该弹窗是本次流程必须新增的审核状态。实现仍使用真实 3 个场景，不补入概念图中的 10 个示意场景。

## P9.1 逐镜字段差异与历史复核

| 项目 | 证据 |
|---|---|
| 桌面历史差异 | `docs/design/script-storyboard-field-diff-history-1440x1024.png`（1440 × 1024，正式 v5 同步记录） |
| 移动端字段差异 | `docs/design/script-storyboard-field-diff-mobile-375x812.png`（375 × 812） |
| Browser 交互 | 剧本开发 → 查看上次同步差异 → 展开 S01 → 切换展开 S02 |
| 数据证据 | 正式 v5 同步记录为 3 个更新；S01 展示标题、画面描述、时长更新和 H3 提示词保留 |

五项视觉对比：

1. 延续选定概念图的近黑蓝画布、暖金主操作、低对比细边框和宋体大标题，没有引入圆角卡片或渐变。
2. 逐镜列表保留工作台的紧凑表格密度；展开详情使用四列结构，不把生产信息改造成卡片瀑布流。
3. 修改后的剧本值以暗金底强调，H3 提示词使用绿色“保留原值”，字段处理可从颜色与文案双重识别。
4. 历史状态明确写出“已执行记录”和执行时间，底部仅保留“已复核，关闭”，避免误以为会再次同步。
5. 375px 下镜头摘要折叠为纵向信息，字段差异变成标签化两列；页面 `scrollWidth` 为 360，小于 375px 视口，无横向溢出。

文案差异检查：原“检查分镜同步”改为更准确的“查看上次同步差异”；新增“当前分镜 / 锁定剧本值 / 本次处理”属于用户要求的字段级审核语义。其余剧本、Agent 与锁定操作未改名。

交互结果：正式 v5 历史记录可展开三个镜头；S01 与 S02 的 `aria-expanded` 会互斥切换；桌面和移动视口均无 console error/warning。历史复核只读，不调用 apply 接口，也不修改正式数据。

有意差异：选定概念图没有逐镜同步模态框；本状态是生产安全门所必需的扩展，仍复用既有字体、色板、图标和矩形容器规则。

final result: passed
